import React from 'react';
import { MonthYear } from './month_year';

const Navigator = ({}) => {
  return (
    <div className="nav">
      <MonthYear />
    </div>
  );
};

export default Navigator;
